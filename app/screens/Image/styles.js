import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    image: {
        height: '100%',
        resizeMode: 'contain',
    },
    text: {
        fontSize: 24,
        fontWeight: 'bold',
        lineHeight: 24,
        padding: 10
    }
});

export default styles;
